# 📚 Book Inventory Table (HTML & CSS)

This project is a simple **Book Inventory web page** built using **HTML and CSS**.  
It displays a list of books with their **title, author, category, reading status, and rating**, styled using CSS selectors, gradients, and attribute-based styling.

---

## ✨ Features

- 📖 Displays book details in a structured table
- 🎨 Different background styles for:
  - **Read**
  - **To Read**
  - **In Progress**
- 🏷️ Status badges styled with gradients
- ⭐ Visual rating system using CSS (1–3 levels)
- 🎯 Uses advanced CSS selectors:
  - Attribute selectors
  - `nth-child`
  - Class-based styling
  
---

## 🧾 HTML Overview

- Uses a `<table>` to organize book data
- Each row has a **status-based class**:
  - `read`
  - `to-read`
  - `in-progress`
- Ratings are represented using nested `<span>` elements:
  - `.one` → 1 rating
  - `.two` → 2 ratings
  - `.three` → 3 ratings




