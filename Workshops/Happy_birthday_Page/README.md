# Greeting Card Workshop

This project is a **Greeting Card** web page created using **HTML** and **CSS**. The page displays a visually appealing birthday card with interactive links to “Send” and “Share” the card. The project demonstrates fundamental web development skills including layout, styling, hover effects, and basic interactivity using anchor links and CSS pseudo-classes.

## 📌 Purpose

* Practice creating visually appealing web components using HTML and CSS
* Apply **CSS transitions**, **hover effects**, and pseudo-classes
* Create a responsive and interactive card design
* Understand section targeting with the `:target` pseudo-class for simple interactivity

## 🛠 Technologies Used

* **HTML5** – For page structure and semantic markup
* **CSS3** – For styling, hover effects, animations, and layout
* **No JavaScript** – Interactivity achieved purely with CSS

## 📂 Project Structure

* `index.html` – Contains the greeting card structure and content
* `Happy_Birthday.css` – Contains all styling rules for layout, hover effects, and interactivity

## 📋 Features

* **Greeting Card** with a headline and custom message
* **Hover Effects**: card scales and background color changes on hover
* **Interactive Links**:

  * “Send Card” link displays a confirmation section
  * “Share on Social Media” link displays a sharing section
* **CSS Pseudo-classes**: `:hover`, `:active`, `:focus`, `:visited` for interactive styling
* **Emoji Decorations** for a festive feel
* **Responsive Design** for different screen sizes
* **Targeted Sections** using `:target` pseudo-class to show/hide content

This README highlights both the **design** and **technical skills** demonstrated in the project, making it suitable for GitHub or portfolio presentation.

